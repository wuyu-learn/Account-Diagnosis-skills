interface:
  display_name: "账户诊断总控"
  short_description: "解析主 Agent 的问账户路由，选择 deferred 卡片并输出账户域局部 JSON"
  default_prompt: "使用 $account-diagnosis 解析主 Agent 分发的完整 Route Extract JSON，处理问账户场景并输出账户域局部 JSON。"
